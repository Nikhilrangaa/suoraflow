"""
visual_search_service.py — Deferred (Phase 6+).

CLIP-based visual search over video frames. Not implemented in Phases 0–5.
"""


def search_by_image(query_image_path: str, project_id: str, top_k: int = 10):
    raise NotImplementedError(
        "Visual/CLIP search is a deferred feature (Phase 6+). "
        "Complete Phases 0–5 before implementing this."
    )
