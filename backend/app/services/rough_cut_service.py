"""
rough_cut_service.py — Deferred (Phase 6+).

Automatic rough-cut assembly from semantic search results. Not implemented in Phases 0–5.
"""
from typing import Any


def generate_rough_cut(project_id: str, query: str) -> Any:
    raise NotImplementedError(
        "Rough-cut generation is a deferred feature (Phase 6+). "
        "Complete Phases 0–5 before implementing this."
    )
